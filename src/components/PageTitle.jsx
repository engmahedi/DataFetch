



const PageTitle = ({title}) => {

  return (
    <div>
      <h2 className="text-5xl text-white flex justify-center items-center py-10">{title }</h2>
      
    </div>
  );
};

export default PageTitle;